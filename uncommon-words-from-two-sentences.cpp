class Solution {
public:
    vector<string> uncommonFromSentences(string s1, string s2) {
        unordered_set<string>st1,st2;
        int n = s1.length();
        int m = s2.length();
        string s = "";
        for(int i = 0;i<n;i++)
        {
            s+=s1[i];
            
        }
    }
};
